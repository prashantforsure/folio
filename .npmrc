# Node 22 LTS is a contract, not a hard gate: see engines in package.json.
# Left non-strict so a local 22.x below 22.11 can still install and warn.
engine-strict=false
# Workspace packages always resolve to the local source, never the registry.
link-workspace-packages=true
prefer-workspace-packages=true
