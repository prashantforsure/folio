/**
 * @folio/ui — primitives, tokens, theme. Presentational only: no data
 * fetching, no domain knowledge.
 *
 * Deliberately empty. No tokens and no components yet. When tokens land they
 * go under src/tokens/, which is the one path eslint.config.mjs exempts from
 * the hardcoded-hex ban.
 */
export const PACKAGE_NAME = '@folio/ui'
