/**
 * @folio/db — Drizzle schema, forward-only migrations, project-scoped
 * repositories. The only place SQL lives.
 *
 * Deliberately empty. No schema, no drizzle.config.ts and no migrations yet:
 * the first migration belongs to the first vertical slice, not to the scaffold.
 */
export const PACKAGE_NAME = '@folio/db'
