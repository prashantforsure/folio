# ADR 0000 — Template

- **Status:** Template (copy, do not edit in place)
- **Date:** YYYY-MM-DD
- **Deciders:**
- **Supersedes / superseded by:** —

> Copy this file to `docs/adr/NNNN-short-kebab-title.md`, keep the number
> sequential, and leave this template untouched.
>
> AGENTS.md requires an ADR — not a commit — for: any change to the node
> schema, a breaking change to `packages/contracts`, and `/app/filmmaking`.
> Resolving anything in the *Open decisions* table also lands here.

## Context

What forces are in play? What is true today that makes this a decision rather
than a preference? Name the constraint that the decision is answering to.

## Decision

One paragraph, in the active voice. What we are doing.

## Consequences

What becomes easier. What becomes harder. What is now foreclosed.

## Alternatives considered

Each with the reason it lost. An alternative with no stated reason is an
alternative that was not considered.

## Does this keep the script authoritative?

The one question this project always asks (AGENTS.md, Role). If this decision
lets some other surface become the source of truth for something the node list
already knows, say so explicitly and justify it.
