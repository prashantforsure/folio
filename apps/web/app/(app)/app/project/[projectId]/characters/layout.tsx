/**
 * The Characters route has no context column since its second pass
 * (2026-09-14): the card grid is the list. The layout is the page.
 */
const Layout = ({ children }: LayoutProps<'/app/project/[projectId]/characters'>) => children

export default Layout
